export enum RegulationMode {
  Schedule = 1, // Regular schedule mode
  Temporary = 2, // Temporary comfort mode (for a few hours/days)
  Permanent = 3, // Permanent manual override
  Away = 4, // Away mode (for a vacation or long absence)
}
